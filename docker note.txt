
FROM php:7.1-fpm

RUN apt-get update && apt-get install -y \
        libfreetype6-dev \
        libmcrypt-dev \
        libpng-dev \
        libjpeg-dev \
        libpng-dev 
RUN docker-php-ext-install iconv mcrypt \
    && docker-php-ext-configure gd --with-freetype-dir=/usr/include/ --with-jpeg-dir=/usr/include/ \
    && docker-php-ext-install gd
	
FROM wyveo/nginx-php-fpm:latest
		
docker run -it -v $PWD/:/var wyveo/nginx-php-fpm:latest bash
docker run -it -d -p 80:80 --name=ppp -v $PWD/:/var ttt bash

docker run -it ttt bash

docker run -d -p 80:80 --name=ppp2 -v $PWD/:/usr/share/nginx/html/ ttt 

docker run -it -d -p 80:80 --name=ppp -v $PWD/:/usr/share/nginx/html/ ttt3 bash
	
	
		
RUN docker-php-ext-configure gd \
        --enable-gd-native-ttf \
        --with-freetype-dir=/usr/include/freetype2 \
        --with-png-dir=/usr/include \
        --with-jpeg-dir=/usr/include
WORKDIR /usr/local/bin
RUN docker-php-ext-install gd


docker build -t wyveo/nginx-php-fpm:latest ./

docker exec -it -v $PWD/html:/ tt bash

docker run -it -v $PWD/:/var wyveo/nginx-php-fpm:latest bash


unzip kodexplorer4.24.zip

docker run -d --name=apache -p 8080:80 -v $HOME/html:/var/www/html php:apache


sudo docker run -d -p 80:80 --name=tt wyveo/nginx-php-fpm:latest 


 docker run -d -p 80:80 --name=mynginx -v $PWD/html:/usr/share/nginx/html nginx
 
 docker run --name=abc -p 80:80 -it nginx bash
 
 docker run --name some-wordpress -p 80:80 -d -it wordpress bash
 
 web:
    image: nginx:latest
    ports:
        - "8080:80"
    volumes:
        - ./html:/usr/share/nginx/html
		
		
		
		/etc/nginx/conf.d/
		
		